<div id="notification">
  <div class="close">close me</div>
  <p>
    <%= message.body %><br>
    from <%= message.user.name %>
  </p>
  <p><%= link_to 'Take me there now!', hotel_path(:id => message.conversation.hotel.id, :conversation_id => message.conversation.id) %></p>

</div>
